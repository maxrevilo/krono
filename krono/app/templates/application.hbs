
<div class="record-view-wrapper">
  <h1>Haz tu pedido!</h1>

  <p>
    (Graba la tienda, productos, cantidad, referencia y marca de lo que necesitas)
  </p>

  {{!-- {{#unless isRecording}} --}}
  {{kr-btn-record start='record' stop='stop'}}
  {{!-- <a class='btn-record' {{action 'record' on='touchStart'}} {{action 'stop' on='touchEnd'}}></a> --}}
  {{!-- {{else}}
  <a class='btn-record-stop' {{action 'stop'}}>Stop</a>
  {{/unless}} --}}
  <br>
  <button {{action 'play'}}>Play</button>
  <button {{action 'upload'}}>Upload</button>

  {{outlet}}
</div>a
